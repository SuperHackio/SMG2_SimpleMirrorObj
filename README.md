# SMG2_SimpleMirrorObj

Works the same way it does in SMG1, so reference it when making your own mirror surfaces.

Add your mirror surface objects as `SimpleMirrorObj` in the *ProductMapObjDataTable*.
